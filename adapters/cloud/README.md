# Cloud Adapter

Status: import-only contract.

This adapter may ingest cloud change, resource, and audit metadata. It must not
perform cloud mutations, store cloud secrets, or become a cloud control plane.

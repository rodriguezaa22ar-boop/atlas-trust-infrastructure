# Scanner Adapter

Status: import-only contract.

This adapter may ingest scanner finding metadata and artifact references. It
must not store raw finding bodies, exploit payloads, credentials, or private
target data.
